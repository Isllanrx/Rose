# Quality tooling for CI and local checks (not bundled into Rose.exe).
# Pinned so the ratchet baseline in .github/quality-baseline.json stays comparable.
ruff==0.16.8
mypy==2.3.1
pip-audit==2.10.1
zizmor==1.30.1
actionlint-py==1.7.12.24
